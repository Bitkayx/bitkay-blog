
---
import Layout from "../layouts/Layout.astro";
import { experience ,hobbies } from "../data/personalInfo.js";
---

<Layout title="Acerca de">
  <!-- Header profesional con foto opcional -->
  <header class="about-header mb-12">
    <div class="flex flex-col lg:flex-row items-center lg:items-start gap-6">
      <!-- Foto de perfil GitHub -->
      <div class="shrink-0">
        <img
          src="https://github.com/Bitkayx.png"
          alt="Jhonatan Elias García Ortega"
          class="w-32 h-32 lg:w-40 lg:h-40 rounded-full border-4 border-slate-200 dark:border-slate-700"
          onerror="this.style.display='none'; document.getElementById('name-section').classList.add('lg:col-span-2');"
        />
      </div>

      <!-- Información profesional -->
      <div id="name-section" class="text-center lg:text-left flex-grow lg:col-span-2">
        <h1 class="text-4xl font-light mb-2">Jhonatan Elias García Ortega</h1>
        <p class="text-xl text-slate-600 dark:text-slate-400 mb-2">
          Frontend Engineer (SSR) | Enterprise & Banking Systems
        </p>
        <p class="text-slate-500 dark:text-slate-500 mb-4">
          📍 Ciudad de México, México
        </p>
        <div class="flex flex-wrap justify-center lg:justify-start gap-2">
          <a href="https://github.com/Bitkayx" target="_blank" class="text-sm px-3 py-1 border border-slate-300 dark:border-slate-600 rounded-full text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors">GitHub</a>
          <span class="text-sm px-3 py-1 border border-slate-300 dark:border-slate-600 rounded-full text-slate-600 dark:text-slate-400">LinkedIn</span>
        </div>
      </div>
    </div>
  </header>

  <!-- Bio profesional -->
  <section class="mb-12">
    <h2 class="text-2xl font-light mb-6">Sobre Mí</h2>
    <div class="prose prose-slate dark:prose-invert max-w-none">
      <p class="text-lg leading-relaxed">
        Fullstack Developer con experiencia en entornos corporativos y bancarios,
        enfocado en la construcción de interfaces claras, mantenibles y seguras
        para productos de alto impacto.
      </p>
      <p>
        He trabajado en el desarrollo de aplicaciones frontend para banca, utilizando
        JavaScript y Web Components, colaborando con equipos multidisciplinarios y
        siguiendo estándares de calidad, accesibilidad y consistencia visual.
      </p>
      <p>
        Mi enfoque está en crear interfaces que reduzcan errores del usuario, faciliten
        procesos complejos y mejoren la experiencia en flujos críticos como formularios,
        dashboards y sistemas internos. Aunque tengo base en backend, mi especialidad
        es el frontend con criterio UX/UI.
      </p>
      <p>
        Entiendo cómo las decisiones de interfaz impactan directamente en la operación,
        la seguridad y la confianza del usuario. Actualmente me intereso seguir creciendo
        en productos financieros o corporativos, donde la claridad, la estabilidad y
        la experiencia de usuario son clave.
      </p>
    </div>
  </section>

  <!-- Experiencia profesional -->
  <section class="mb-12">
    <h2 class="text-2xl font-light mb-6">Experiencia Profesional</h2>
    <div class="space-y-8">
      {experience.map((job, index) => (
        <div class="relative border-l-2 border-slate-200 dark:border-slate-700 pl-6 ml-3">
          <!-- Punto en timeline -->
          <div class="absolute -left-2 top-2 w-4 h-4 bg-slate-600 dark:bg-slate-400 rounded-full border-2 border-white dark:border-slate-900"></div>

          <div class="bg-white dark:bg-slate-800 rounded-lg border border-slate-200 dark:border-slate-700 p-6 hover:shadow-md transition-shadow">
            <div class="flex flex-col lg:flex-row lg:justify-between mb-2">
              <div>
                <h3 class="text-xl font-medium text-slate-900 dark:text-slate-100">{job.company}</h3>
                <p class="text-slate-600 dark:text-slate-400">{job.position}</p>
              </div>
              <div class="text-right text-sm text-slate-500 dark:text-slate-500 mt-2 lg:mt-0">
                <p>{job.period}</p>
                <p>{job.duration}</p>
                <p>{job.location}</p>
              </div>
            </div>

            <div class="flex flex-wrap gap-1 mb-3">
              {job.technologies.map(tech => (
                <span class="px-2 py-1 text-xs border border-slate-300 dark:border-slate-600 rounded text-slate-600 dark:text-slate-400 bg-slate-50 dark:bg-slate-800">
                  {tech}
                </span>
              ))}
            </div>

            <p class="text-slate-600 dark:text-slate-400 text-sm">{job.description}</p>
          </div>
        </div>
      ))}
    </div>
  </section>

  <!-- Hobbies -->
  <section>
    <h2 class="text-2xl font-light mb-6">Cuando no estoy programando, estos son algunos de mis hobbies 🚀</h2>
    <div class="grid grid-cols-2 sm:grid-cols-4 gap-4">
      {hobbies.map(hobby => (
        <div class="text-center p-4 bg-slate-50 dark:bg-slate-800 rounded-lg border border-slate-200 dark:border-slate-700">
          <div class="text-2xl mb-2">{hobby.icon}</div>
          <p class="text-sm text-slate-600 dark:text-slate-400">{hobby.name}</p>
        </div>
      ))}
    </div>
  </section>
</Layout>
